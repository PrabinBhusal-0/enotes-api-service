spring.application.name=Enote

spring.datasource.url=jdbc:mysql://localhost:3306/enote
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=1234
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=none
#industry basic ddl-auto should not be update and create //because if there is update then jpa will update the table and if there is create then the jap will always creates a new tables and deletes existing table //if in production level some mistakes are maden in the code then the database of the industary can be damaged so do not use create and update

